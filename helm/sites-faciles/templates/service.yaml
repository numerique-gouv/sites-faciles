apiVersion: v1
kind: Service
metadata:
  name: {{ include "sites-faciles.fullname" . }}
  labels:
    {{- include "sites-faciles.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sites-faciles.selectorLabels" . | nindent 4 }}
