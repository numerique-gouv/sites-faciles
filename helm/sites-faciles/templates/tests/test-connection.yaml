apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sites-faciles.fullname" . }}-test-connection"
  labels:
    {{- include "sites-faciles.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sites-faciles.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
